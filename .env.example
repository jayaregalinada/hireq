GOOGLE_GENERATIVE_AI_API_KEY=     # Optional — get free key at ai.google.dev (may require billing outside US)
GROQ_API_KEY=                     # Optional — get free key at console.groq.com (no billing required)
AI_MODEL=groq                     # Required — set to 'gemini' or 'groq' (defaults to 'groq')
